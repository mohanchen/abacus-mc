# Unit tests for this module live in unittests/ and are wired from
# source/source_estate/CMakeLists.txt (same pattern as module_charge/unittests),
# so this module-level CMakeLists is intentionally left without test logic.
